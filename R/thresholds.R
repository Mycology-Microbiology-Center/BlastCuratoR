
#' Pre-configured curation thresholds
#'
#' A named list of default threshold values (percent identity, bitscore, E-value, etc.)
#' used internally by `apply_thresholds()` and `curate_blast()`.
#' Stored in `R/sysdata.rda` and lazy‐loaded at package load.
#' @keywords internal
"thresholds"
